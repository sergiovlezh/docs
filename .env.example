# Database
# For Docker: SQLite stored in the db-data volume
DATABASE_URL=sqlite:////data/db/docs.db

# Security — replace with a long random secret before running
SECRET_KEY=change-me-to-a-long-random-secret

# App
DEBUG=false
TOKEN_EXPIRE_HOURS=12
MIN_PASSWORD_LENGTH=8
